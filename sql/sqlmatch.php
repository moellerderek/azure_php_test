<?php

$date = ['date'];

foreach ($astro as $key => $value) {

$selfnrange=self-10;
$selfprange=self+10;

$selfpc=$selfnrange-60;
$selfnc=$selfprange-60;

$selfpcc=$selfnrange-120;
$selfncc=$selfprange-120;

$selfpccc=$selfnrange+120;
$selfnccc=$selfprange+120;
	
}

return ($selfnrange, $selfprange, $selfpc, $selfnc, $selfpcc, $selfncc, $selfpccc, $selfnccc);

if ($deg =< 360) {
$reseult = $deg;
}
else {
	$result = $deg - 360;
}
